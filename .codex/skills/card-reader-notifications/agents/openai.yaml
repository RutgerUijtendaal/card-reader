interface:
  display_name: "Card Reader Notifications"
  short_description: "Maintain Card Reader notification flows"
  default_prompt: "Use $card-reader-notifications to add or change notification types, inbox behavior, or future delivery channels."
