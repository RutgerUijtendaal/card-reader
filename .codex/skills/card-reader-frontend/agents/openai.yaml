interface:
  display_name: "Card Reader Frontend"
  short_description: "Maintain the Card Reader Vue frontend"
  default_prompt: "Use $card-reader-frontend to update the layered Card Reader Vue frontend while preserving routes, behavior, and theme support."
