{
  "name": "@card-reader/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run --project . python -m card_reader_worker.main",
    "build": "echo \"Python service has no build step\"",
    "test": "echo \"No worker tests yet\"",
    "lint": "uv run --project . ruff check src",
    "typecheck": "uv run --project . mypy src"
  }
}