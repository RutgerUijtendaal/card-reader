{
  "name": "card-reader",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "deps:js": "pnpm install",
    "deps:py": "uv sync --project . --all-packages --all-groups --locked",
    "setup": "pnpm deps:js && pnpm deps:py",
    "bootstrap:dev": "pnpm setup && uv run --project . --package card-reader-api python scripts/bootstrap-dev.py",
    "bootstrap:dev:reset": "pnpm setup && uv run --project . --package card-reader-api python scripts/bootstrap-dev.py --reset",
    "sync:python": "pnpm deps:py",
    "dev": "turbo run dev dev:builder --parallel",
    "dev:all": "turbo run dev dev:builder --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "fix": "turbo run fix",
    "typecheck": "turbo run typecheck",
    "check": "turbo run typecheck && turbo run lint && turbo run test",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.1.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "vue-demi"
    ]
  }
}
