{
  "name": "@card-reader/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "generate": "openapi-typescript http://127.0.0.1:8000/openapi.json -o src/generated/api.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "echo \"No tests for contracts\""
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "eslint": "^9.13.0",
    "openapi-typescript": "^7.4.4",
    "typescript": "^5.6.3"
  }
}